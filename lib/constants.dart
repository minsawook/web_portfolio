import 'package:flutter/material.dart';

// Refined light theme color palette
const primaryColor = Color(0xFF4C7BF2); // vibrant accent
const secondaryColor = Color(0xFFF0F4F8); // card backgrounds
const darkColor = Color(0xFFD9E2EF); // subtle borders & dividers
const bodyTextColor = Color(0xFF333333); // high contrast text
const bgColor = Color(0xFFF9FAFC); // soft page background

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web
